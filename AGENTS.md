# AGENTS.md

> 本文件是 `miku-kumi/miku-kumi.github.io` 的长期开发指南。任何 AI Agent(DeepSeek Harness 等)参与本仓库开发前,必须先通读本文并遵守。
> 内容由开发者本人确认,结合仓库真实状态编写。

## 仓库现状速查(动手前必读)

**这是什么**:mikuumi 的个人主页,GitHub Pages 静态站点。
**访问地址**:自定义域名 `a.gugugagaea.de5.net`(见 `CNAME`),以及 `miku-kumi.github.io`。

| 文件 | 作用 | 硬性注意 |
|---|---|---|
| `index.html` | 整个网站:HTML + CSS + JS 单文件(约 220 行),样式全部内联在 `<style>`,脚本全部内联在 `<script>` | 唯一的页面文件 |
| `CNAME` | 自定义域名 `a.gugugagaea.de5.net` | **永远不要删除或改名**,否则线上域名立即失效 |
| `图像.png` | 头像,Hero 区以圆形裁切展示(`object-fit: cover`) | 中文文件名;重命名必须同步改 `index.html` 里的引用 |

**技术栈**:纯 HTML / CSS / Vanilla JavaScript。零依赖、零构建、零框架。
**部署方式**:`main` 分支 push 后 GitHub Pages 自动发布——**push 前必须在本地验证过**。

**页面结构**(自上而下):Hero(头像 + 渐变标题 + 标签 + CTA 按钮)→ 关于我(bio 卡片 + 信息徽章)→ 兴趣(4 卡片)→ 学习方向(3 卡片)→ 项目(4 卡片)→ 技能(进度条)→ 联系方式(GitHub / X / Gmail 圆形按钮)→ footer。

**已有视觉语言**(后续改动必须与之协调,不允许整体推翻):
- 深色背景 `#050510`;核心渐变色:青色 `#6ee7ff` → 紫色 `#c084fc`
- 星空 canvas 背景:180 颗缓慢上浮的星点,requestAnimationFrame 常驻绘制
- 鼠标光晕跟随(`.cursor-glow`)
- 玻璃拟态卡片:白色低透明度底 + `backdrop-filter: blur`
- 滚动显现动画:IntersectionObserver 把 `.hidden` 切换成 `.show`,同时触发技能进度条填充
- 头像:旋转渐变光环 + 上下浮动动画
- 响应式断点:`768px` 和 `480px` 两档

**当前实现的已知细节**(这是现状备忘,不是待修清单;未经用户要求不要顺手重构):
- 导航只有 4 个锚点(关于 / 兴趣 / 项目 / 联系);"学习方向""技能"两个 section 没有 id、不在导航里
- `.contact-btn` 的 click 被 JS 统一拦截后用 `window.open` 打开,包括 `mailto:` 链接(移动端唤起邮件客户端的行为可能受影响)
- mousemove 光晕直接改 `style.left/top`,未做 rAF 合帧
- 尚无 meta description / favicon / Open Graph 标签(用户提出时再补)

---

## 一、关于开发者

这个项目属于一个偏内向、理性、喜欢自己折腾技术的 INTP 型开发者。

开发者具备一定的:HTML / CSS / JavaScript、Git / GitHub、Linux、VPS / SSH、AI / Agent、电脑硬件基础。不是完全的小白,**不要过度解释最基础的 HTML/CSS/JS 概念**。

开发者特别重视:实际效果、可控性、可修改性、性价比、简洁、实际体验。

不喜欢:没必要的复杂架构、过度工程化、无意义的依赖、为了"显得专业"而增加复杂度、大量解释但没有实际结果。

## 二、项目定位

这是个人主页 / Personal Portfolio / Digital Space。

网站不是普通企业官网,也不是模板化的开发者 Portfolio。

核心感觉:**「属于个人的数字空间」**

整体应该体现:安静、夜晚、科技、二次元、AI、编程、音乐、个人感、沉浸感。

## 三、核心视觉审美

整体视觉方向:**Dark + Purple + Glass + Soft Glow + Anime + Tech**

优先考虑:深色背景、暗紫色、蓝紫色、柔和辉光、玻璃拟态、半透明层、星空 / 夜空、微弱粒子、柔和渐变、精致阴影、轻微动画、克制的科技感。

目前网站已有:星空背景、鼠标光晕、玻璃卡片、蓝紫渐变、Avatar、滚动动画。

后续修改应该尽可能保持这些视觉语言的**一致性**,而不是完全换成另一套设计。

## 四、设计原则

### 1. 克制

不要:到处发光、到处渐变、大量粒子、过度动画、过度毛玻璃、巨大的标题、炫技式 WebGL。

动画应该服务于氛围,而不是抢视觉焦点。

### 2. 高级感优先

宁愿少做一些效果,也不要做得廉价。

设计目标更接近「现代个人数字空间」,而不是「赛博朋克模板网站」。

### 3. 保持呼吸感

页面:不要过度拥挤、留足空白、卡片不要堆得太密、不要让每个元素都成为视觉重点。

## 五、编码原则

目前项目是一个简单的 GitHub Pages 静态网站。

优先保持:HTML、CSS、Vanilla JavaScript。

除非确实有明显收益,否则不要引入:React、Vue、Next.js、Tailwind、大型 UI Framework、构建工具。

**不要为了简单的个人主页把项目改造成复杂前端工程。**

## 六、修改代码时的要求

每次准备修改之前:

1. 先读取相关文件。
2. 理解现有结构。
3. 找出可以复用的代码。
4. 尽量进行局部修改。
5. 不要无理由重写整个项目。

如果必须大改,先说明:为什么需要大改、哪些部分会改变、是否会影响现有功能。

## 七、代码质量

要求:

- HTML 语义合理
- CSS 结构清晰
- JavaScript 不重复绑定事件
- 避免全局变量污染
- 避免不必要的定时器
- 避免持续运行的高开销动画
- 注意移动端性能
- 注意减少布局抖动
- 注意图片加载性能

优先:CSS Animation、transform、opacity。

尽量避免:高频修改 layout 属性、不必要的 scroll 事件高频计算、高频 DOM 查询、大型动画库。

## 八、响应式设计

必须兼顾:手机、平板、PC、大尺寸显示器。不能只针对 1920×1080 设计。

移动端应该重新考虑:导航、字体、卡片、间距、Hero、图片尺寸——而不是单纯把 PC 缩小。

## 九、交互设计

交互应该:自然、柔和、快速、有反馈。

可以使用:hover、微缩放、fade、translate、glow、smooth scroll。

但不要为了"有动画"而添加动画。

## 十、内容审美

网站内容可以围绕:AI、编程、Linux、硬件、音乐、VOCALOID、游戏、动漫、个人项目。

这些内容应该体现真实的个人兴趣,而不是写成标准求职简历。

避免大量使用:"热爱技术"、"追求卓越"、"持续学习"、"优秀团队协作能力"这种模板式企业文案。

更偏向真实、自然、有个人特色的表达。

## 十一、网站开发优先级

以后优化网站时的优先顺序:

1. 功能正确
2. 移动端体验
3. 可访问性
4. 性能
5. 视觉一致性
6. 动画
7. 装饰效果

不要为了视觉效果牺牲:性能、可读性、可维护性、可访问性。

## 十二、Agent 工作方式

当用户提出一个网站修改需求时,首先判断类型:

**A. 小修改**:修改文字、修改颜色、修改按钮、增加卡片、修改布局 → 直接定位并修改。

**B. 中等修改**:新增完整 section、重做导航、重做 Hero、增加主题切换、增加项目展示 → 先分析现有结构,再实施。

**C. 大型修改**:重构页面结构、引入新的数据系统、大量新增功能 → 先检查现有架构,尽量寻找不需要重构的实现方式。

## 十三、非常重要:不要自作主张

不要因为"看起来更现代"就:

- 擅自换掉整体配色
- 擅自删除现有内容
- 擅自更换头像
- 擅自换技术栈
- 擅自加入 npm
- 擅自增加第三方服务
- 擅自增加外部依赖

除非用户明确要求,或者当前方案确实无法实现需求。

## 十四、执行任务时

不要只告诉用户"可以这样做"。应该尽可能直接完成:修改文件、创建文件、删除文件、测试、检查结果。

完成后简要告诉用户:改了什么、为什么这样改、是否测试通过、是否需要注意兼容性。

不要写很长的过程汇报。

## 十五、最终设计关键词

每次设计决策都可以用下面这组关键词进行校准:

> 夜晚 / 深色 / 紫色 / 蓝紫 / 玻璃 / 柔光 / 星空 / 二次元 / 科技 / AI / 安静 / 沉浸 / 个人感 / 精致 / 克制

最终目标:

**「一个真正属于 mikuumi 的数字空间,而不是一个套模板做出来的个人网站。」**

---

## 附:部署与操作守则(硬性规则)

1. **push 即上线**:`main` 分支没有预览环境。任何改动,push 前先在本地浏览器打开 `index.html` 完整自查一遍。
2. **本地验收清单**:至少检查 320 / 375 / 768 / 1024 / 1440 几个视口宽度,以及深色背景下文字对比度。
3. `CNAME` 永远保留在仓库根目录,内容不改。
4. 新增静态资源放在仓库根目录或新建的简单子目录即可;保持零构建、零依赖架构。
5. 提交信息用一行说清改动意图;一次提交聚焦一件事。
6. 不引入 package.json、构建工具、外部 CDN 资源,除非用户明确提出且收益清晰。
7. 注意 `图像.png` 这类中文文件名:URL 中会被百分号编码,重命名时务必同步更新 `index.html` 引用。
